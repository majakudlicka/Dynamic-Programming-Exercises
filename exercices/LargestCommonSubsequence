Input
x1....xn
y1...yn

for i:0->n LI(i, 0) = 0
for j:0->n LI(0,j) = 0

for i:1->n
  for j:1->n

  if (xi = yj)
    LI(i, j) = 1 + LI(i-1, j-1)

  if (xi <> yj)
    LI = Max { LI(i-1, j, LI(i, j-1)}

  return L(n,n)

// Time complexity O(n2)