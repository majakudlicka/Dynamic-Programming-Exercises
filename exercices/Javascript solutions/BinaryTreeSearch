function sumBetweenIndices(array, startIndex, endIndex) {
    let sum = 0;
    for (i = startIndex; i<= endIndex; i++) {
        sum = sum + array[i];
    }
    return sum;
}

sumBetweenIndices([2,3,4,5,6,7,8], 2, 5)